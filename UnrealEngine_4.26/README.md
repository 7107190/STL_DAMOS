# UnrealEngine_4.26

This directory is a placeholder for the external Unreal Engine 4.26 dependency used by the vvu runtime workspace.

The full Unreal Engine checkout is not tracked in this GitHub mirror because it is a large external dependency. On vvu, the runtime path is:

```text
/home/vvu/vv/DAMOS/UnrealEngine_4.26
```

DAMOS-specific Unreal source overlays are tracked under:

```text
Carla-0.9.16-source/Unreal/CarlaUE4/Plugins/Carla/Source/Carla/Damos
```
